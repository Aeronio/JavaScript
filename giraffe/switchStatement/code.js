// Switch statements are the same

var myGrade = "A";

//Switch syntax
switch(myGrade){
    case "A":
        document.write("You pass!");
        break;
    case "F":
        document.write("Oops!");
        break;
    default:
        document.write("Invalid grade");
}